type goalPropretys = 'tittle' | 'description' | 'link' | 'creatorEmail';
type goalType = {
    tittle: string;
    description: string;
    link: string;
    creatorEmail: string;
};

export type { goalPropretys, goalType };
